import React from "react";
import Form from "./Form";
// import Badge from "./Badge";
// import BadgeList from "./BadgeList";

function App() {
    // const badges = BadgeList.map(badge => <Badge key={badge.id} info={badge}/>)
    
    return (
        <div>
            <Form />
            {/* {badges} */}
        </div>
    );
};

export default App;